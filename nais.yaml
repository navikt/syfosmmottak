name: "syfosmmottak"
image: "repo.adeo.no:5443/syfo/syfosmmottak"
replicas:
  min: 1
  max: 4
  cpuThresholdPercentage: 90
port: 8080
healthcheck:
  liveness:
    path: /is_alive
    # wait 30 seconds before trying to check status,
    # and then try 30 times with 5 second intervals before
    # killing the pod: 30 + 5*30 = 180 seconds before pod is killed
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 30
  readiness:
    path: /is_ready
    initialDelay: 30
    periodSeconds: 5
    timeout: 5
prometheus:
  enabled: true
  path: /prometheus
resources:
  limits:
    cpu: 250m
    memory: 1Gi
  requests:
    cpu: 100m
    memory: 128Mi
redis:
  enabled: true
fasitResources:
  used:
    - alias: srvsyfosmmottak
      resourceType: Credential
    - alias: mqGateway03
      resourceType: QueueManager
    - alias: syfomottak_channel
      resourceType: Channel
    - alias: syfomottak_backout_queue
      resourceType: queue
#    - alias: syfomottak_input_queue
#      resourceType: Queue
    - alias: syfosmmottak_tmp_routed_input
      resourceType: Queue
      propertyMap:
        queueName: MOTTAK_QUEUE_UTSENDING_QUEUENAME
#    - alias: mottak_queue_utsending
#      resourceType: Queue
#      propertyMap:
#        queueName: MOTTAK_QUEUE_UTSENDING_QUEUENAME
    - alias: kafka_bootstrap_servers
      resourceType: baseurl
